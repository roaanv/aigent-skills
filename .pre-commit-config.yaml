# Pre-commit hooks configuration
# Run `make setup` or `pre-commit install` to activate hooks locally.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
